# OPENCODE_ROADMAP.md

## 1. ملخص الوضع الحالي

يُظهر قالب Blogger XML (`Blogarch_lite_audited-3.xml`) مستوى عالٍ من التعقيد ويتبع أفضل ممارسات تطوير الويب الحديثة في تحسين الأداء، تحسين محركات البحث (SEO)، وإمكانية الوصول. ومع ذلك، فإن التركيز الأساسي للتحسين يكمن في **تنظيم الشيفرة وقابلية الصيانة**، خاصةً فيما يتعلق بالاستخدام المكثف لـ **JavaScript المضمن (inline) و CSS المضمن (inline `style` attributes)**.

تمت محاولة سابقة لإعادة هيكلة الشيفرة (refactoring) بهدف استخراج JavaScript المضمن إلى ملفات خارجية. لكن تقرير التحقق (`VERIFICATION_REPORT.md`) يُشير إلى أن عملية إعادة الهيكلة **غير مكتملة وغير قابلة للنشر حاليًا**. الملف الرئيسي `Blogarch_lite_audited-3.xml` لا يزال يحتوي على جميع السكريبتات المضمنة الأصلية، والوحدات الخارجية الجديدة المُشار إليها في هذا الملف تُرجع خطأ 404 عند محاولة تحميلها من شبكة توصيل المحتوى (CDN) لأنها لم تُرفع بعد. بالإضافة إلى ذلك، تم تحديد عدة أخطاء حرجة ومخاطر محتملة في الوحدات المستخرجة تتعلق بالتنفيذ المزدوج، مشاكل النطاق (scope)، الميزات المفقودة، وتلوث واجهة برمجة التطبيقات (API) العامة.

## 2. الفجوات الرئيسية

*   **استخراج غير مكتمل للسكريبتات:** لا يزال قالب Blogger XML الرئيسي يحتوي على جميع سكريبتات JavaScript المضمنة الأصلية، مما يجعل عملية إعادة الهيكلة جزئية.
*   **فجوة النشر (Deployment Gap):** وحدات JavaScript المستخرجة في مجلد `src/` لم تُرفع بعد إلى شبكة توصيل المحتوى (CDN)، مما يؤدي إلى أخطاء 404 عند محاولة تحميلها.
*   **تراجعات وظيفية وأخطاء:**
    *   يحتوي `homepage-data.js` على أخطاء `ReferenceError` بسبب عدم إمكانية الوصول إلى المتغيرات (`_lastFocus`, `trapFocus`).
    *   تفتقر `theme.js` إلى مستمع لحدث تغيير حجم النافذة (resize listener) لوظيفة `buildStars`.
    *   لا يضمن استخدام خاصية `defer` للسكريبتات المُضافة ديناميكيًا ترتيب التحميل، وهو أمر حاسم للوحدات ذات التبعيات المتبادلة.
*   **فجوات جودة الشيفرة وقابلية الصيانة:**
    *   غياب آليات منع التنفيذ المزدوج (deduplication guards) في الوحدات الجديدة قد يؤدي إلى تنفيذ كل ميزة مرتين.
    *   تغيير ضمني في واجهة برمجة التطبيقات (API) العامة (`window.BlogArch`) بواسطة الوحدات، مما يؤدي إلى تضارب محتمل.
    *   لا تزال سمات `style` المضمنة بحاجة إلى التحويل إلى فئات CSS خارجية.

## 3. المخاطر المحتملة

*   **فشل النشر:** سيؤدي نشر ملف `Blogarch_lite_audited-3.xml` الحالي إلى 20 خطأ HTTP 404 لملفات JavaScript ولن يحدث أي تغيير وظيفي بسبب عدم إزالة السكريبتات المضمنة.
*   **تعطل حرج للتطبيق:** في حال نشر الوحدات الخارجية دون إزالة السكريبتات المضمنة وإصلاح الأخطاء، ستتعطل الوظائف الأساسية مثل تبديل الثيمات، والعرض، والنوافذ المنبثقة، أو ستعمل بشكل غير صحيح بسبب التنفيذ المزدوج وأخطاء `ReferenceError`.
*   **تدهور الأداء:** سيؤثر التنفيذ المزدوج للسكريبتات سلبًا على الأداء.
*   **ديون الصيانة:** ستؤدي إعادة الهيكلة غير المكتملة والأخطاء غير المعالجة إلى زيادة عبء الصيانة المستقبلي.
*   **ثغرات أمنية:** لا يزال وكيل Google Apps Script بحاجة إلى تدقيق أمني.
*   **انتهاكات الخصوصية:** استخدام `localStorage` دون موافقة صريحة.

## 4. الأولويات

### P0: إصلاحات حرجة وإعادة هيكلة أساسية (يجب إنجازها فورًا)

*   **إزالة JavaScript المضمن:** إزالة شيفرة JavaScript المضمنة من `Blogarch_lite_audited-3.xml` لجميع الوحدات المستخرجة (مع الإبقاء على السكريبتات الحرجة فقط وفقًا لخطة إعادة الهيكلة وتقرير التحقق).
*   **رفع ملفات الوحدات:** رفع جميع ملفات وحدات JavaScript المستخرجة من `src/` إلى شبكة توصيل المحتوى (CDN) (`https://cdn.jsdelivr.net/gh/AchRafAyaOu/blogs_arch@main`).
*   **إصلاح `homepage-data.js`:** تصحيح الخطأ في `homepage-data.js` عن طريق تصدير `_lastFocus` و `trapFocus` من `drawer.js` أو جعلها متاحة بشكل صحيح.
*   **إضافة آليات منع التنفيذ المزدوج (init guards):** إضافة آليات حماية لكل وحدة جديدة لمنع التنفيذ المزدوج.
*   **استعادة مستمع `resize`:** استعادة مستمع `resize` في `theme.js` لوظيفة `buildStars`.
*   **ضمان ترتيب التحميل:** فرض ترتيب تحميل حتمي للسكريبتات المُضافة ديناميكيًا (على سبيل المثال، التحميل المتسلسل `async=false` أو تجميع الملفات بترتيب التبعيات).
*   **مواءمة واجهة API العامة:** مواءمة واجهة API العامة للوحدات (`window.BlogArch.*`) مع الملفات الموجودة (`blogarch.js`, `Blogarch.lessons.js`).

### P1: تحسينات أساسية وجودة الشيفرة (يجب إنجازها قريبًا)

*   **تحويل أنماط `style` المضمنة:** تحويل سمات `style` المضمنة المتكررة والثابتة إلى فئات CSS خارجية (المرحلة 3 من خطة إعادة الهيكلة).
*   **تدقيق أمني لوكيل Google Apps Script:** إجراء تدقيق أمني لوكيل Google Apps Script المستخدم للنماذج.
*   **تنظيف CSS:** دمج قواعد CSS المكررة والتحقق من استخدام متغيرات الألوان (المرحلة 4 من خطة إعادة الهيكلة).

### P2: مهمة ولكن لا تعيق العمل (يمكن إنجازها لاحقًا)

*   **سياسة الخصوصية للمخزن المحلي:** تطبيق سياسة خصوصية واضحة أو لافتة موافقة المستخدم لاستخدام `localStorage` (يتطلب تدقيقًا للامتثال القانوني).
*   **إعادة هيكلة منطق البحث:** إعادة هيكلة منطق البحث إلى وحدة متماسكة لمعالجة أي منطق مكرر محتمل.
*   **تحسين كتل CSS الكبيرة المضمنة:** التحسين المستمر لكتل CSS الكبيرة المضمنة في `<b:skin>` (جهد مستمر ويخضع لقيود المنصة).

### P3: تحسينات طفيفة (أولوية منخفضة)

*   **إزالة الشيفرة الميتة:** إزالة الشيفرة الميتة `_origAddOpen` في `src/core/homepage-data.js`.

## 5. خطة تنفيذ مختصرة

1.  **العودة إلى الوضع السابق وتخطيط P0:** التراجع عن ملف `Blogarch_lite_audited-3.xml` الحالي إلى `Blogarch_lite_audited-3.xml.backup` لضمان الاستقرار. إنشاء خطة فرعية مفصلة لمهام P0، مع معالجة كل خطأ وخطوة نشر.
2.  **تنفيذ P0 (تكراريًا):**
    *   لكل وحدة: إزالة نظيرها المضمن من `Blogarch_lite_audited-3.xml`.
    *   تنفيذ إصلاحات الأخطاء (النطاق، مستمع `resize`) وآليات الحماية في ملفات `src/*.js`.
    *   تحديث `Blogarch_lite_audited-3.xml` للإشارة بشكل صحيح إلى الوحدات الخارجية في `ASSETS.js` (مع ضمان المسارات الصحيحة وتحديد الإصدارات).
    *   **مهم جدًا:** رفع ملفات `src/*.js` المحدثة إلى شبكة توصيل المحتوى (CDN).
    *   التحقق الشامل من كل تغيير باستخدام قائمة التحقق الكاملة للتحقق (`REFACTOR_PLAN.md`).
3.  **تنفيذ P1:** تنفيذ إعادة هيكلة CSS، وتحويل الأنماط المضمنة، وإجراء التدقيق الأمني لسكريبتات التطبيقات.
4.  **تنفيذ P2 و P3 (حسب توفر الموارد):** معالجة قضايا الخصوصية، إعادة هيكلة البحث، وتحسين CSS.
5.  **المراقبة المستمرة:** التحقق بانتظام من وظائف الموقع، الأداء، وسجلات الأخطاء بعد النشر.

## 6. معايير القبول النهائية

*   نشر `Blogarch_lite_audited-3.xml` على Blogger دون أي شيفرة JavaScript مضمنة تتوافق مع الوحدات المستخرجة (باستثناء الحرجة منها).
*   تحميل جميع وحدات JavaScript الخارجية (`src/*.js`) بنجاح من شبكة توصيل المحتوى (CDN) (لا توجد أخطاء 404).
*   جميع الميزات (كما هو مدرج في قائمة التحقق للتحقق في `REFACTOR_PLAN.md`) تعمل بشكل صحيح على كل من الأجهزة المحمولة والمكتبية.
*   لا يوجد تنفيذ مزدوج لأي ميزة JavaScript.
*   لا توجد أخطاء `ReferenceError` أو أي أخطاء JavaScript حرجة أخرى في وحدة التحكم (console).
*   تعمل وظائف Learn Modal المتعلقة بالتركيز واستعادته بشكل صحيح.
*   تتجدد النجوم في الوضع الداكن عند تغيير حجم النافذة.
*   تكون واجهة API العامة `window.BlogArch` متسقة وخالية من أي تضارب غير مقصود.
*   تحويل جميع أنماط `style` المضمنة الثابتة المتكررة إلى فئات CSS.
*   مراجعة أمن وكيل Google Apps Script والتأكد من قوته.
*   الحفاظ على الأداء العام للصفحة (LCP, FID, CLS) أو تحسينه.